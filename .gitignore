node_modules/
dist/
.astro/
.DS_Store
*.log

# Keep originals + private capture data out of the repo
originals/
capture/.env
capture/private/
*.private.json

# Git tracks CODE only — submitted content (entries + uploaded media) lives in
# the live store / locally, and is assembled into the static build at deploy
# ("freeze") time. One example of each is kept so the repo builds on a fresh clone.
src/content/entries/*.json
!src/content/entries/2026-06-14-example.json
public/media/*
!public/media/2026-06-14-example/

# Large video assets (e.g. background loop) — host via CDN/object storage or
# commit an optimized mp4 deliberately, rather than bloating git with the source.
public/img/*.mov
public/img/*.mp4
public/img/*.webm
